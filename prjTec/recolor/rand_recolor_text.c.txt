
#include <time.h>

srand(time(NULL));

// V2
color cells[SIZE * SIZE];
for (int i = 0; i < SIZE * SIZE; i++) {
    cells[i] = rand() % NB_COLORS;
}

// for run program in program
else if (choice == 'c') {  // Run bin for create new game
    system(argv[0]);
}
