#include <stdio.h>
#include <stdlib.h>
#include "game.h"


int main(void) {
        bool win = false;
        game g = game_new_empty();

        while (!win)
        {
                game_
        }





        //game g = game_new(RED, 12);
        game_delete(g);
        return EXIT_SUCCESS;
}
