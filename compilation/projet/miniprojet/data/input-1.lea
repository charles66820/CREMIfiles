class main {
	m: list<string>;

	main(args: list<string>) {
		for (i: integer := 6 ; i <= 12 ; ) {
      for ( j: integer := 0 ; j < 10 ; j++) {
        writeln(j);
      }
      i++;
    }
    k: integer
    for (;;) {
      if (k > 140) break;
      writeln(k);
      k++;
    }
    for ( ; ; ) { }
  }
}

