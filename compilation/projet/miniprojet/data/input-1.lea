class main {
	m: list<string>;

	main(args: list<string>) {
    k: integer;
		for (i: integer := 6 ; i <= 12 ; ) {
      for ( j: integer := 0 ; j < 10 ; j++) {
        writeln(j);
      }
      i++;
    }
    for (;;) {
      if (k > 140) break;
      writeln(k);
      k++;
    }
    for ( ; ; ) { }
  }
}

