class main {
	m: list<string>;

	main(args: list<string>) {
		for (i: integer := 6 ; i <= 12 ; ) {
      for ( j: integer := 0 ; j < 10 ; j++) {
        writeln(j);
      }
      i++;
    }
    for (;;) {
      if (i > 140) break;
      writeln(i);
      i++;
    }
    for ( ; ; ) { }
  }
}

