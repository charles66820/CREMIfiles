.pos 0

# p = &x
irmovl x, %esi # met l'addresse de x dans esi

# a = *p
mrmovl (%esi), %eax # lie la valeur de l'adresse stoker dans esi dans eax
rmmovl %eax, a

# *p = b
mrmovl b, %eax
rmmovl %eax,(%esi)

# p = &b
irmovl b,%esi


# (*p) ++
mrmovl (%esi),%eax
iaddl 1, %eax
rmmovl %eax, (%esi)

halt

.pos 0x100
a:  .long 0
b:  .long 2
x:  .long 3

